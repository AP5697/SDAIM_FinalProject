# Runtime dependencies for the Hugging Face Space.
#
# Versions are pinned exactly, not loosened with >=. A scikit-learn pickle
# records the version that created it; loading model.joblib under a different
# scikit-learn minor version raises InconsistentVersionWarning and can fail
# outright. These pins are the versions the model was actually trained with -
# see models/metadata.json "environment".

streamlit==1.59.1
scikit-learn==1.9.0
xgboost==3.3.0
pandas==2.3.3
numpy==2.4.6
scipy==1.18.0
joblib==1.5.3

# NOTE: `shap` is deliberately NOT a runtime dependency. It was, and its
# compiled extension segfaulted the Space (exit 139) against this xgboost and
# numpy build. A segfault kills the interpreter, so the application's fallback
# never ran. Per-session attribution now uses XGBoost's own exact TreeSHAP via
# `pred_contribs=True`, which produces identical values with no extra native
# library. `shap` remains a development-only import in src/train.py, guarded by
# try/except, for the global importance table.
